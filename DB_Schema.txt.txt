-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.
-- Regenerated from the live prod DB on 2026-09-12 via scripts/_psql.mjs against information_schema/pg_catalog.
-- Column types are the information_schema rendering (e.g. USER-DEFINED shows the udt name).

CREATE TABLE public.activities (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  bottle_id uuid NOT NULL,
  variant_id uuid,
  action text NOT NULL,
  pour_type text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  session_id uuid,
  details jsonb);

CREATE TABLE public.announcements (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text NOT NULL,
  body text NOT NULL,
  coach_id text,
  published boolean NOT NULL DEFAULT false,
  audience text NOT NULL DEFAULT 'all'::text,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  published_at timestamp with time zone);

CREATE TABLE public.bottle_variants (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  bottles_id uuid NOT NULL,
  frontimage_url text,
  backimage_url text,
  age text,
  proof numeric,
  batch text,
  release_year integer,
  store_pick_name text,
  verified boolean NOT NULL DEFAULT false,
  created_by uuid,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_by uuid,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  notes text,
  elo_global numeric DEFAULT 1500,
  nose text,
  palate text,
  finish text,
  is_default boolean NOT NULL DEFAULT false,
  is_catchall boolean NOT NULL DEFAULT false,
  store_pick_of_variant_id uuid,
  shelf_ready boolean NOT NULL DEFAULT false,
  image_reject_reason_ids ARRAY NOT NULL DEFAULT '{}'::uuid[],
  image_review_note text,
  image_reviewed_at timestamp with time zone,
  image_reviewed_by uuid,
  image_flagged_at timestamp with time zone,
  image_flagged_by uuid,
  image_flag_note text,
  bottle_height numeric,
  bottle_height_source text);

CREATE TABLE public.bottles (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  category text,
  verified boolean DEFAULT false,
  elo_global numeric DEFAULT 1500,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  distillery text,
  style text,
  barcode text,
  created_by uuid,
  updated_by uuid,
  nose text,
  palate text,
  finish text,
  extras text,
  proof numeric,
  age text,
  volume text,
  frontimage_url text,
  backimage_url text,
  variant_axis text,
  variant_triage text,
  variant_triaged_at timestamp with time zone,
  variant_triaged_by uuid);

CREATE TABLE public.events (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid,
  session_id text,
  event_type text NOT NULL,
  surface text,
  target_type text,
  target_id text,
  metadata jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now());

CREATE TABLE public.feedback (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  type text NOT NULL,
  message text NOT NULL,
  screenshot_url text,
  screenshot_path text,
  status text NOT NULL DEFAULT 'new'::text,
  submitted_by uuid NOT NULL,
  reviewed_by uuid,
  admin_note text,
  user_agent text,
  viewport text,
  route text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone);

CREATE TABLE public.image_reject_reasons (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  slug text NOT NULL,
  label text NOT NULL,
  sort_order integer NOT NULL DEFAULT 100,
  active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid);

CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_by uuid,
  title text NOT NULL,
  body text NOT NULL,
  url text,
  audience text NOT NULL DEFAULT 'everyone'::text,
  target_user_id uuid,
  sent_count integer NOT NULL DEFAULT 0,
  failed_count integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now());

CREATE TABLE public.post_comments (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  activity_id uuid NOT NULL,
  user_id uuid NOT NULL,
  parent_id uuid,
  body text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone);

CREATE TABLE public.post_reactions (
  activity_id uuid NOT NULL,
  user_id uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now());

CREATE TABLE public.push_subscriptions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  endpoint text NOT NULL,
  p256dh text NOT NULL,
  auth text NOT NULL,
  user_agent text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  last_used_at timestamp with time zone);

CREATE TABLE public.suggested_edits (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  bottle_id uuid NOT NULL,
  variant_id uuid,
  target_table text NOT NULL,
  field text NOT NULL,
  old_value text,
  new_value text,
  status text NOT NULL DEFAULT 'pending'::text,
  submitted_by uuid NOT NULL,
  reviewed_by uuid,
  review_note text,
  submission_group uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  reviewed_at timestamp with time zone);

CREATE TABLE public.tasting_details (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tasting_session_id uuid NOT NULL,
  bottle_id uuid NOT NULL,
  notes jsonb,
  created_at timestamp with time zone DEFAULT now(),
  variant_id uuid,
  pour_index integer,
  glass_letter text,
  rank integer);

CREATE TABLE public.tasting_results (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tasting_session_id uuid NOT NULL,
  winner_bottle_id uuid NOT NULL,
  loser_bottle_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  winner_variant_id uuid,
  loser_variant_id uuid);

CREATE TABLE public.tasting_sessions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text,
  bottle_ids ARRAY NOT NULL DEFAULT '{}'::uuid[],
  is_blind boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  variant_ids ARRAY NOT NULL DEFAULT '{}'::uuid[],
  mode text);

CREATE TABLE public.user_bottles (
  user_id uuid NOT NULL,
  bottle_id uuid NOT NULL,
  rating_stars numeric DEFAULT NULL::numeric,
  elo numeric DEFAULT 1500,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  currently_owned boolean DEFAULT true,
  variant_id uuid,
  times_had integer NOT NULL DEFAULT 1,
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  owned_count integer NOT NULL DEFAULT 0,
  emptied_count integer NOT NULL DEFAULT 0,
  tasted_at timestamp with time zone,
  blind_tasted_at timestamp with time zone);

CREATE TABLE public.user_ratings (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  bottle_id uuid NOT NULL,
  variant_id uuid NOT NULL,
  stars numeric NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  note text);

CREATE TABLE public.user_relationships (
  from_user_id uuid NOT NULL,
  to_user_id uuid NOT NULL,
  kind text NOT NULL,
  notify_kinds ARRAY NOT NULL DEFAULT '{}'::text[],
  created_at timestamp with time zone NOT NULL DEFAULT now());

CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  username text NOT NULL,
  email text NOT NULL,
  collection ARRAY DEFAULT '{}'::uuid[],
  created_at timestamp with time zone DEFAULT now(),
  auth_id uuid,
  role text NOT NULL DEFAULT 'user'::text,
  seen_coach_ids ARRAY NOT NULL DEFAULT '{}'::text[],
  notify_push boolean NOT NULL DEFAULT true,
  notify_prompt_optout boolean NOT NULL DEFAULT false,
  account_type text NOT NULL DEFAULT 'human'::text,
  avatar_url text,
  feed_default text NOT NULL DEFAULT 'everyone'::text,
  notify_reactions boolean NOT NULL DEFAULT true);

CREATE TABLE public.wishlists (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  bottle_id uuid NOT NULL,
  variant_id uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now()
);

-- Views
--   all_bottle_details  [security_invoker=true]
--   all_variant_details  [security_invoker=true]
--   bottle_scores  [runs as owner]
--   my_variant_scores  [security_invoker=true]
--   variant_scores  [runs as owner]

-- Functions
--   actor_muted_by_post_owner(p_activity_id uuid, p_actor_id uuid)  SECURITY DEFINER
--   bottle_delete_impact(p_bottle uuid)  SECURITY DEFINER
--   commit_bottle_import(payload jsonb)  SECURITY DEFINER
--   default_to_admin()  SECURITY DEFINER
--   delete_user_cascade(target_user_id uuid)  SECURITY DEFINER
--   elo_global_target(p_variant uuid)
--   elo_k_for_meetings(p_prior_meetings integer)
--   elo_star(p_elo numeric)
--   email_exists(p_email text)  SECURITY DEFINER
--   flag_variant_image(p_variant uuid, p_note text)  SECURITY DEFINER
--   guard_event_insert()
--   is_admin()  SECURITY DEFINER
--   merge_bottle(p_source uuid, p_target uuid, p_variant_map jsonb, p_confirm_name text)  SECURITY DEFINER
--   merge_variant_into(p_source uuid, p_target uuid)  SECURITY DEFINER
--   protect_comment_update()
--   protect_image_review()  SECURITY DEFINER
--   protect_submission_update()  SECURITY DEFINER
--   protect_user_role()  SECURITY DEFINER
--   purge_bottle(p_bottle uuid, p_confirm_name text)  SECURITY DEFINER
--   replay_elo_history()  SECURITY DEFINER
--   split_bottle_into_variants(p_bottle uuid, p_axis text)
--   stamp_blind_tasted_for_session()
--   stamp_tasted_for_pour()
--   update_elo_after_result()
--   update_elo_for_session()
--   update_timestamp()  SECURITY DEFINER
--   variant_delete_impact(p_variant uuid)  SECURITY DEFINER
--   variant_guess_avg(variant_ids uuid[])  SECURITY DEFINER

-- Triggers
--   trig_stamp_tasted_after_pour on activities
--   trg_default_to_admin_bottle_attr on bottle_variants
--   trg_protect_image_review on bottle_variants
--   trg_update_timestamp_bottle_attr on bottle_variants
--   trg_default_to_admin_bottles on bottles
--   trg_update_timestamp_bottles on bottles
--   trg_guard_event_insert on events
--   trg_protect_feedback_update on feedback
--   trg_protect_comment_update on post_comments
--   trg_protect_suggested_edits_update on suggested_edits
--   trig_update_elo_after_session on tasting_results
--   trig_zz_stamp_tasted_after_session on tasting_results
--   trg_protect_user_role on users

-- RLS policies
--   activities: activities_delete_own (DELETE)
--   activities: activities_insert_own (INSERT)
--   activities: activities_select_authenticated (SELECT)
--   announcements: Admin manage announcements (ALL)
--   announcements: Read published announcements (SELECT)
--   bottle_variants: admins can delete variants (DELETE)
--   bottle_variants: admins delete variants (DELETE)
--   bottle_variants: Auth insert variants (INSERT)
--   bottle_variants: Public read (SELECT)
--   bottle_variants: admins read variants (SELECT)
--   bottle_variants: Own unverified update variants (UPDATE)
--   bottle_variants: admins can update variants (UPDATE)
--   bottle_variants: admins update variants (UPDATE)
--   bottles: admins can delete bottles (DELETE)
--   bottles: admins delete bottles (DELETE)
--   bottles: Auth insert bottles (INSERT)
--   bottles: Public read bottles (SELECT)
--   bottles: Own unverified update bottles (UPDATE)
--   bottles: admins can update bottles (UPDATE)
--   bottles: admins update bottles (UPDATE)
--   events: events_insert_anon (INSERT)
--   events: events_insert_auth (INSERT)
--   events: events_select_admin (SELECT)
--   feedback: feedback_insert_own (INSERT)
--   feedback: feedback_select (SELECT)
--   feedback: feedback_update (UPDATE)
--   image_reject_reasons: admins write reasons (ALL)
--   image_reject_reasons: read reject reasons (SELECT)
--   notifications: Admin read notifications (SELECT)
--   post_comments: post_comments_insert_own (INSERT)
--   post_comments: post_comments_select (SELECT)
--   post_comments: post_comments_soft_delete (UPDATE)
--   post_reactions: post_reactions_delete_own (DELETE)
--   post_reactions: post_reactions_insert_own (INSERT)
--   post_reactions: post_reactions_select (SELECT)
--   push_subscriptions: Delete own subscription (DELETE)
--   push_subscriptions: Insert own subscription (INSERT)
--   push_subscriptions: Select own subscriptions (SELECT)
--   push_subscriptions: Update own subscription (UPDATE)
--   suggested_edits: suggested_edits_insert_own (INSERT)
--   suggested_edits: suggested_edits_select (SELECT)
--   suggested_edits: suggested_edits_update (UPDATE)
--   tasting_details: Users can manage their own tasting details (ALL)
--   tasting_details: admins read all tasting_details (SELECT)
--   tasting_results: Users insert own tasting results (INSERT)
--   tasting_results: Users read own tasting results (SELECT)
--   tasting_results: admins read all tasting_results (SELECT)
--   tasting_sessions: Users can manage their own tasting sessions (ALL)
--   tasting_sessions: admins read all tasting_sessions (SELECT)
--   user_bottles: Users can manage their own bottle data (ALL)
--   user_bottles: admins read all user_bottles (SELECT)
--   user_ratings: user_ratings_delete_own (DELETE)
--   user_ratings: user_ratings_insert_own (INSERT)
--   user_ratings: user_ratings_select (SELECT)
--   user_ratings: user_ratings_update_own (UPDATE)
--   user_relationships: user_relationships_delete_own (DELETE)
--   user_relationships: user_relationships_insert_own (INSERT)
--   user_relationships: user_relationships_select (SELECT)
--   user_relationships: user_relationships_update_own (UPDATE)
--   users: Users own their data (ALL)
--   users: Allow anon insert via auth (INSERT)
--   users: Users can insert their own profile (INSERT)
--   users: Public read users (SELECT)
--   users: Users can view their own profile (SELECT)
--   users: Users can update their own profile (UPDATE)
--   users: Users update own via auth (UPDATE)
--   wishlists: wishlists_delete_own (DELETE)
--   wishlists: wishlists_insert_own (INSERT)
--   wishlists: wishlists_select (SELECT)
