"use client";

export default function UsersTab({ currentPublicUserId: _ }: { currentPublicUserId: string }) {
  return (
    <div className="text-sm text-gray-500">
      Users tab — coming next commit.
    </div>
  );
}
